# portfolio-site

Workspace memory and routing live at `../../AGENTS.md` (`~/projects/AGENTS.md`); read that first and follow its memory routing.
This directory lives in `davidajudua/workspace`; its own docs are the source of truth for working in it: see `CLAUDE.md`, `CONTEXT.md`, and `docs/agents/`.
