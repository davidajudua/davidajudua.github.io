# Bison board

Bison overwrites `feed.json` when `school/packets/LATEST.md` changes.
Same three buckets (ready / submitted / upcoming), plus optional `submitted_archive` for older DONE rows.
No second memory.
